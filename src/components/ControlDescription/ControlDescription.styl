/**
 * ControlDescription
 */

.container
	padding 16px
	border-top 1px solid rgba(0,0,0,.1)

.checkbox
	margin-bottom 24px

.checkbox_label
	font-weight 600
	margin-bottom 1.5em
	display block

.radio

	label:first-child
		font-weight 600
		margin-bottom 1.5em

	&.no-is_active
		pointer-events none
		opacity .3
